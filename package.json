{
  "name": "babel-typescrpit-playground",
  "license": "GPL-3.0",
  "scripts": {
    "build:js": "npx babel src/ --out-dir lib/ --delete-dir-on-start --presets=@babel/preset-env",
    "build:ts": "npx babel src/ --out-dir lib/ --delete-dir-on-start --extensions '.ts'",
    "start": "node lib/"
  },
  "dependencies": {
    "@babel/polyfill": "^7.6.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/preset-env": "^7.6.3",
    "@babel/preset-typescript": "^7.6.0"
  }
}
